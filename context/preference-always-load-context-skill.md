# Mandate for Context Learning Protocol

## Type
Preference

## Date
2026-02-14

## Description
The user has mandated that the `context-learning-protocol` skill must be loaded and followed for every single interaction.

## Context/Reasoning
To ensure that no preferences or learnings are lost and that the project's history is meticulously documented in `/context/`.

## Actionable Item
- Always call `activate_skill(name: 'context-learning-protocol')` at the beginning of a session if not already active.
- Strictly adhere to the `/context/` saving workflow for every task.
